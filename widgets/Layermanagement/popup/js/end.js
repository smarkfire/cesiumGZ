$(function () {
    laydate.render({
        elem: '#timeend' //指定元素
        ,type: 'datetime'
    });
    laydate.render({
        elem: '#inpend' //指定元素
        ,type: 'datetime'
    });
    laydate.render({
        elem: '#inpend_zr' //指定元素
    });
})