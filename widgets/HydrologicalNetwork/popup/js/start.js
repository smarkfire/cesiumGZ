$(function () {
    laydate.render({
        elem: '#timestart', //指定元素
        type: 'datetime'
    });
    laydate.render({
        elem: '#inpstart' //指定元素
        ,type: 'datetime'
    });
    laydate.render({
        elem: '#inpstart_zr' //指定元素
    });
})